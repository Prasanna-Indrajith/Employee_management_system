# Database Scripts

Project SQL files are organized by purpose:

- `schema/` - schema extensions and feature-specific database structures.
- `seed/` - data population scripts for local development and testing.
- `maintenance/` - repair or one-off database maintenance scripts.
- `legacy/` - archived scripts for removed or historical features.

Run scripts from the repository root so relative paths in documentation stay consistent.
